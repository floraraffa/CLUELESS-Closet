- !<AssetImportMetadata/b841a77c-eb28-456d-b12f-60cb1848e4ce>
  ImportedAssetIds:
    TypeScriptAsset: !<reference> fbae7ca3-a168-402f-881b-fa6523ac56a4
  ImporterName: TypeScriptAssetImporter
  PrimaryAsset: !<reference> fbae7ca3-a168-402f-881b-fa6523ac56a4
  PackageType: NotAPackage
  LegacyPackagePolicy: ~
  ExtraData:
    {}
  AssetDataMap:
    TypeScriptAsset: !<own> 640553d5-1414-4e43-9cda-eb6e52a4d34f
  DependentFiles:
    []
  ImporterSettings: !<AssetImporterSettings>
    {}
  CompressionSettings: !<own> 00000000-0000-0000-0000-000000000000
- !<TypeScriptAssetData/640553d5-1414-4e43-9cda-eb6e52a4d34f>
  SvgIcon: ""
  SetupScript:
    code: ""
  Description: ""
  VersionMajor: 0
  VersionMinor: 0
  VersionPatch: 0
  ComponentUid: db6f7c06-ce03-4464-a5ff-e46b0f59f2f2
  ExportUid: 00000000-0000-0000-0000-000000000000
  PackagePolicy: CanBeUnpacked
  ScriptInputsHidden:
    cardCollectionContainer: false
    carouselBackScale: false
    carouselHideThreshold: false
    carouselRadius: false
    carouselSwipeSensitivity: false
    collectionCardScale: false
    enableGridGesture: false
    enablePinchGrabAssist: false
    gridCardScale: false
    gridColumns: false
    gridCurveDepth: false
    gridForwardOffset: false
    gridGestureEnterAngle: false
    gridGestureExitAngle: false
    gridOffsetX: false
    gridOffsetY: false
    gridOffsetZ: false
    gridSpacingX: false
    gridSpacingY: false
    invertCarouselSwipeDirection: false
    leftPalmAnchor: false
    pickedCardScale: false
    pinchBlockSwipeDistanceCm: false
    pinchGrabRadiusCm: false
    pinchReleaseDistanceCm: false
    pinchStartDistanceCm: false
    rightIndexTip: false
    rightThumbTip: false
  ScriptTypesHidden:
    {}
  ReadMe: !<reference> 00000000-0000-0000-0000-000000000000
  DeclarationFile: !<reference> 00000000-0000-0000-0000-000000000000
  Tags:
    []
  Attachments:
    []
  DefaultScriptInputs:
    - rightIndexTip: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      rightThumbTip: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      cardCollectionContainer: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      leftPalmAnchor: !<reference.SceneObject> 00000000-0000-0000-0000-000000000000
      collectionCardScale: 0.230000
      pickedCardScale: 0.360000
      carouselRadius: 6.500000
      carouselSwipeSensitivity: 0.650000
      pinchBlockSwipeDistanceCm: 7.500000
      invertCarouselSwipeDirection: false
      enablePinchGrabAssist: true
      pinchStartDistanceCm: 2.800000
      pinchReleaseDistanceCm: !<float> 4.000000
      pinchGrabRadiusCm: !<float> 16.000000
      carouselHideThreshold: 0.750000
      carouselBackScale: 0.600000
      enableGridGesture: true
      gridGestureEnterAngle: !<float> 60.000000
      gridGestureExitAngle: !<float> 40.000000
      gridColumns: !<float> 5.000000
      gridSpacingX: !<float> 9.000000
      gridSpacingY: !<float> 11.000000
      gridCardScale: 0.220000
      gridForwardOffset: !<float> 25.000000
      gridOffsetX: !<float> 0.000000
      gridOffsetY: !<float> 0.000000
      gridOffsetZ: !<float> 0.000000
      gridCurveDepth: !<float> 6.000000
  ScriptTypes:
    -
      {}
  InputLines:
    - '// @input SceneObject rightIndexTip {"hint":"Right hand index finger tip SceneObject (index-3_end joint)"}'
    - '// @input SceneObject rightThumbTip {"hint":"Right hand thumb tip SceneObject (thumb-3_end joint)"}'
    - '// @input SceneObject cardCollectionContainer {"hint":"Card Collection Container — SceneObject under left wrist"}'
    - '// @input SceneObject leftPalmAnchor {"hint":"Left palm anchor SceneObject (for fallback hand position)"}'
    - '// @input float collectionCardScale = 0.23 {"hint":"Scale of cards inside the carousel (small). Default: 0.18"}'
    - '// @input float pickedCardScale = 0.36 {"hint":"Scale of cards when picked / placed in world (big). Default: 0.36"}'
    - '// @input float carouselRadius = 6.5 {"hint":"Minimum radius (cm) of the carousel circle. Default: 2.5"}'
    - '// @input float carouselSwipeSensitivity = 0.65 {"hint":"Sensitivity of right-hand swipe to rotate carousel (0 = off, 1 = normal)"}'
    - '// @input float pinchBlockSwipeDistanceCm = 7.5 {"hint":"Index–thumb distance (cm) below which swipe is blocked (pinch or near-pinch). Increase if carousel still rotates when approaching to grab."}'
    - '// @input bool invertCarouselSwipeDirection {"hint":"If enabled, carousel only rotates counter-clockwise from swipe; otherwise clockwise only."}'
    - '// @input bool enablePinchGrabAssist = true {"hint":"Enable pinch grab assist when Frame grab is hard to trigger"}'
    - '// @input float pinchStartDistanceCm = 2.8 {"hint":"Pinch start distance (cm) between index and thumb"}'
    - '// @input float pinchReleaseDistanceCm = 4 {"hint":"Pinch release distance (cm) between index and thumb"}'
    - '// @input float pinchGrabRadiusCm = 16 {"hint":"Max distance (cm) from pinch center to card to start assist grab"}'
    - '// @input float carouselHideThreshold = 0.75 {"hint":"Hide when sin(angle) < -this (0 = only fade, no hide). Higher = pop/depop further behind wrist. Default 0.5 ≈ ~20° extra hidden each side."}'
    - '// @input float carouselBackScale = 0.6 {"hint":"Depth emphasis: back/side cards shrink to this fraction of the front card (1 = no depth, 0.6 = back cards 60% size). Front/focused card stays full size for readability."}'
    - '// @input bool enableGridGesture = true {"hint":"Open the LEFT palm outward (away from you) while the collection is open to fan all cards into a grid that follows your hand. 0 = feature off."}'
    - '// @input float gridGestureEnterAngle = 60 {"hint":"Palm-facing angle (deg) above which the open-palm-outward gesture activates. >30 = palm faces away from you. Default 60."}'
    - '// @input float gridGestureExitAngle = 40 {"hint":"Palm-facing angle (deg) below which grid mode releases (hysteresis, < enter). Default 40."}'
    - '// @input float gridColumns = 5 {"hint":"Number of columns in the grid array. Default 5."}'
    - '// @input float gridSpacingX = 13 {"hint":"Horizontal spacing (cm) between grid cards. Default 13."}'
    - '// @input float gridSpacingY = 16 {"hint":"Vertical spacing (cm) between grid cards. Default 16."}'
    - '// @input float gridCardScale = 0.4 {"hint":"Card scale in grid mode. Default 0.4"}'
    - '// @input float gridForwardOffset = 25 {"hint":"How far (cm) to push the grid wall away from your eyes, beyond the palm. Higher = further. Default 25."}'
    - '// @input float gridOffsetX {"hint":"Grid offset X (cm) — shifts the whole array right (+) / left (-) relative to the hand."}'
    - '// @input float gridOffsetY {"hint":"Grid offset Y (cm) — shifts the whole array up (+) / down (-) relative to the hand."}'
    - '// @input float gridOffsetZ {"hint":"Grid offset Z (cm) — shifts the whole array further (+) / closer (-) on top of gridForwardOffset."}'
    - '// @input float gridCurveDepth = 8 {"hint":"Concave curve depth (cm): how far the grid edges wrap toward you. 0 = flat. Default 8."}'
